import { StyleSheet } from "react-native";

export default StyleSheet.create({
  home: {
    maxWidth: 60,
    maxHeight: 60,
    position: "relative",
    top: 3,
    resizeMode: "contain",
  },
});
